``` python
def solution(nums):
    ln = len(nums)
    set_nums = set(nums)
    ls = len(set_nums)
    
    if ln/2 >= ls:
        answer = ls
    else:
        answer = ln/2
    
    return answer
```
